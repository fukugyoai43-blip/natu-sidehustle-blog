import type { MetadataRoute } from "next"; import { getAllPosts } from "@/lib/posts"; import { categories } from "@/lib/categories"; import { siteConfig } from "@/site.config";
export default function sitemap():MetadataRoute.Sitemap{const base=siteConfig.url;const fixed=['','/posts','/about','/profile','/recommendations','/contact','/privacy'].map(path=>({url:`${base}${path}`,lastModified:new Date()}));const postUrls=getAllPosts().map(p=>({url:`${base}/posts/${p.slug}`,lastModified:new Date(p.updatedAt||p.publishedAt)}));const categoryUrls=categories.map(c=>({url:`${base}/categories/${c.slug}`,lastModified:new Date()}));return[...fixed,...postUrls,...categoryUrls]}
