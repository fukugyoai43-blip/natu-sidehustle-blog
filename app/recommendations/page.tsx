import type { Metadata } from "next"; import { siteConfig } from "@/site.config"; export const metadata:Metadata={title:"おすすめ・商品",description:"natuのおすすめ・商品への案内ページ。",alternates:{canonical:"/recommendations"}};
export default function Recommendations(){const items=[['note',siteConfig.links.note],['Brain',siteConfig.links.brain],['BOOTH',siteConfig.links.booth],['Gumroad',siteConfig.links.gumroad],['楽天ROOM',siteConfig.links.rakutenRoom]];return <div className="shell page prose"><p className="eyebrow">おすすめ・商品</p><h1>おすすめ・商品</h1><p>実際に作った商品や、紹介したいものを掲載するページです。リンク先の内容・価格・条件が分かる形でご案内します。</p><div className="link-list">{items.map(([label,href])=>href?<a key={label} href={href} target="_blank" rel="noopener noreferrer">{label}を見る →</a>:<p key={label}>{label}：準備中</p>)}</div></div>}
